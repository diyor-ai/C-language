#include <stdio.h>

int main(void)
{
    /*
    With to symbolles , we can write a comment that spans multiple lines.
    */

    int son = 5;
    float pi = 3.14;
    char harf = 'A';

    printf("Son: %d\n", son);
    printf("PI: %f\n", pi);
    printf("Harf: %c\n", harf);

    return 0;
}