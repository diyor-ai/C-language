#include <stdio.h>

int main ()
{
	unsigned int a = -1;
	unsigned int b = 600;
	if (a > b)
	{
		printf("%d is greater than %d\n", a, b);
	}
	else{
		printf("%d is smaller than %d\n", a, b);
	}
	return 0;
}
